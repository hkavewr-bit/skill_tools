# 状态与恢复

暂停、交接或上下文过长时才写简短检查点；短任务无需进度文件。
优先沿用项目现有的权威进度来源，没有则使用任务进度.md。旧项目的项目进度.md 可继续使用，不强制迁移。
已有 handover 状态时读取其有效状态，不创建平行账本，也不自动调用其完整交接流程。

记录范围、已完成、关键决定、路径、最近验证、阻塞及下一动作。恢复时核对相关代码与修改，差异再定向调查。
无固定同步次数、审批状态链或必需文档清单。范围或契约变化时更新相关既有说明；不重写整套文档。
